# Translation API Configuration
# Default: MyMemory Translation API (free, no API key required)
# You can replace this with any translation service that accepts GET requests
# Format: https://api.mymemory.translated.net/get?q=TEXT&langpair=SOURCE|TARGET
VITE_TRANSLATION_API_URL=https://api.mymemory.translated.net/get

# Example with other services:
# Google Translate API (requires API key):
# VITE_TRANSLATION_API_URL=https://translation.googleapis.com/language/translate/v2

# DeepL API (requires API key):
# VITE_TRANSLATION_API_URL=https://api-free.deepl.com/v2/translate

# Backend Canister ID Configuration
# For production deployments, provide the backend canister ID via one of these methods:
# 1. Build-time environment variables (recommended for static builds):
VITE_CANISTER_ID_BACKEND=your-backend-canister-id-here
# or
VITE_BACKEND_CANISTER_ID=your-backend-canister-id-here

# 2. Runtime configuration (alternative for dynamic deployments):
# Create a file at public/env.json with the following structure:
# {
#   "CANISTER_ID_BACKEND": "your-backend-canister-id-here"
# }
# This file will be loaded at runtime and made available via window.__ENV__
